# PivotHacks

Hackathon project repo (https://github.com/notjackl3/pivothacks).

## Status

Freshly initialized. No framework or tooling chosen yet. Update this file once the stack is decided
(build command, dev command, test command, folder layout).

## Working conventions

- Work on `main` unless told otherwise; keep commits small and descriptive.
- Do not commit secrets. Put env vars in `.env.local` (gitignored) and document required keys here.
- Use non-interactive commands (`--yes`) when scaffolding.
